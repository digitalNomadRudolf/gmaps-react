import React from "react";

import "./intro.scss";

const Intro = () => (
    <div className="intro-container">
        <h2 className="intro-h2">
            Our Mission
        </h2>
        <p className="intro-p">
            This is a Google Maps API app where you can see the location of the company on the map.
        </p>
    </div>
);

export default Intro;